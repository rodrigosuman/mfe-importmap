import React from "react";
import ReactDOM from "react-dom/client";
import MotorBusca from "./components/motor-busca";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <MotorBusca />
  </React.StrictMode>
);
